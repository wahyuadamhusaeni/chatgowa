PORT=3000
BASIC_AUTH_USER=admin
BASIC_AUTH_PASS=yourpassword

# Local development
DATABASE_URL="file:./prisma/dev.db"

# Docker (override in .env for Docker)
# DATABASE_URL="file:/app/prisma/dev.db"

# Timeout (in milliseconds) for forwarding webhook to GoWA.
# If GoWA does not respond within this time, the request is aborted.
# Default: 10000 (10 seconds)
FETCH_TIMEOUT_MS=10000
